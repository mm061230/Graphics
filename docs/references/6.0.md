机械制图数字化复刻与Agent状态机令牌锁执行协议 (V6.0 状态机硬门禁版)0. 状态机总控引擎机制 (State Machine Engine)本协议的核心执行逻辑为有向无环图（DAG）串行状态机。Agent 在全量执行过程中，必须严格按照 GATE 1 ➔ GATE 2 ➔ GATE 3 ➔ GATE 4 的顺序单向推进。硬性红线：禁止跨阶段生成。Agent 在未完整输出当前阶段的“解锁令牌（Unlock Token）”前，上下文中绝对不允许出现下一阶段的逻辑、坐标数据或代码。若前置依赖未满足，必须立刻触发回滚（Rollback）中断生成。[输入原图] 
   │
   ▼
[GATE 1: 几何提炼锁] ──(未通过)──> [异常拦截/引导优化]
   │ (产出: TOKEN_GEOM_EXTRACT_PASS)
   ▼
[GATE 2: 投影代数锁] ──(未通过)──> [坐标重算/重对齐]
   │ (产出: TOKEN_PROJ_ALIGN_PASS)
   ▼
[GATE 3: 拓扑剖切锁] ──(未通过)──> [面域布尔重构]
   │ (产出: TOKEN_TOPO_SECTION_PASS)
   ▼
[GATE 4: 军事总门禁] ──(单元测试断言)──> [代码编译与矢量交付]
1. GATE 1：原图几何提炼锁 (Original Image Geometry Extraction Lock)激活条件：用户上传原图，触发初始化。执行任务：Agent 必须调用逆向视觉管道，对原图进行刚体对齐，并全量提取非结构化几何特征，严禁进行任何投影合理性推导。数据交付物：必须在上下文中显式输出以下标准的物理度量 JSON 字典：JSON{
  "gate_1_status": "EXTRACTED",
  "image_metrics": {
    "rotation_theta_deg": 1.2,
    "perspective_distortion_rate": 0.005,
    "gray_contrast": 95
  },
  "raw_features": {
    "detected_views_count": 2,
    "view_bounding_boxes": [[100, 500, 400, 900], [100, 100, 400, 400]],
    "explicit_centerlines": [{"start": [250, 950], "end": [250, 50]}],
    "explicit_section_lines": [{"label": "A-A", "type": "FULL", "direction": "VERTICAL"}]
  }
}
审计门禁断言：断言 1：rotation_theta_deg 必须 $\le 5.0$。断言 2：detected_views_count 必须 $\ge 1$。解锁令牌：当且仅当上述断言全部为 True 时，在当前生成行尾输出：[TOKEN: TOKEN_GEOM_EXTRACT_PASS] （产生此令牌后，GATE 1 锁定，GATE 2 激活）2. GATE 2：投影代数守恒锁 (Projection Algebraic Invariant Lock)激活条件：检测到上下文中存在 [TOKEN: TOKEN_GEOM_EXTRACT_PASS]。执行任务：基于 GATE 1 提取的原始几何特征，应用空间投影守恒方程组，全面审计多视图之间的“长对正、高平齐、宽相等”对应关系，找出漏线、错线坐标。空间代数守恒方程组应用：主-俯 [长对正] 约束：$X_{\text{Front}} = X_{\text{Top}}$主-左 [高平齐] 约束：$Y_{\text{Front}} = Y_{\text{Left}}$俯-左 [宽相等] 偏差守恒：$$\Delta = |Y_{\text{Top}} - Y_{\text{Top\_Center}}| = |X_{\text{Left}} - X_{\text{Left\_Center}}|$$数据交付物：输出经投影守恒修正后的完备多视图拓扑关联矩阵：JSON{
  "gate_2_status": "CONSERVED",
  "algebraic_audit": {
    "length_alignment_error_px": 0.0,
    "height_alignment_error_px": 0.0,
    "width_equality_error_px": 0.0
  },
  "missing_line_analysis": [
    {
      "view": "FRONT",
      "reason": "Missing visible internal step matching Top View edge",
      "action": "ADD",
      "geometry": {"type": "LINE", "style": "VISIBLE_OUTLINE", "coords": [100, 500, 400, 500]}
    }
  ]
}
解锁令牌：当且仅当所有对齐误差 error_px 严格等于 0.0 时，在当前生成行尾输出：[TOKEN: TOKEN_PROJ_ALIGN_PASS] （产生此令牌后，GATE 2 锁定，GATE 3 激活）3. GATE 3：剖切拓扑逻辑锁 (Topology Sectioning Logic Lock)激活条件：检测到上下文中存在 [TOKEN: TOKEN_PROJ_ALIGN_PASS]。执行任务：执行三维实体剖切面域的布尔运算，处理全剖、半剖或局部剖的线型升级与填充范围。在此阶段专门拦截三大机械原理性高频错误。三大核心拦截算法规则：加强筋纵向不剖原则：若剖切面通过肋板、轮辐的纵向对称面，肋板区域的 line_style 必须设置为 VISIBLE_OUTLINE 封闭多边形，且将其从 layer_hatch（剖面线层）中求差集剔除。波浪线隔离准则：WAVE_LINE 必须在实体内部连续，一旦遇到圆孔、键槽等中空多边形（Holes），必须物理断开，严禁在空气中悬空渲染。半剖视图虚线清除规则：在半剖视图的外形侧，必须调用 delete() 移出所有 HIDDEN_OUTLINE（细虚线），保持外观整洁。数据交付物：显式输出通过 Shapely 拓扑计算的面域闭合数据：JSON{
  "gate_3_status": "TOPOLOGY_SECTIONED",
  "interception_check": {
    "longitudinal_rib_detected": false,
    "rib_hatch_area_intersection": 0.0,
    "waveline_air_intersection": 0.0
  },
  "hatch_boundaries": {
    "outer_polygon_count": 1,
    "inner_holes_subtracted_count": 2
  }
}
解锁令牌：当且仅当拦截指标中 intersection 面积严格为 0.0 时，在当前生成行尾输出：[TOKEN: TOKEN_TOPO_SECTION_PASS] （产生此令牌后，GATE 3 锁定，GATE 4 激活）4. GATE 4：军事化总门禁与代码执行释放 (Military Final Gatekeeper)激活条件：检测到上下文中存在 [TOKEN: TOKEN_TOPO_SECTION_PASS]。执行任务：将前三步收敛的 JSON 几何实体正式输入物理图层，在隔离的代码执行环境中编译图层堆叠（Layering Stack），并执行物理硬断言单元测试。图层堆叠物理顺序（由底至顶）：layer_centerline（最底层，规避红色点画线覆盖轮廓）layer_hatch（剖面线填充层）layer_hidden_outline（细虚线层）layer_visible_outline（粗实线轮廓层，置顶保证边界凌厉）layer_text_symbols（文字标注与剖切符号层）硬断言脚本（Python 级硬门禁）：Pythondef verify_strict_standards(shapely_layers):
    # 断言 1：中心线垂直交叉准则（圆心相交处必须是长划与长划相交，严禁空隙或短点相交）
    for cross_point in shapely_layers["CENTERLINE_INTERSECTIONS"]:
        assert cross_point.is_on_long_dash == True, "Error: 圆心定位点画线未在长划处相交！"
        
    # 断言 2：中心线外延合法性（中心线必须伸出实体边界外 3~5mm，像素空间对应 15~35px）
    for cline in shapely_layers["CENTERLINE"]:
        visible_bound = shapely_layers["VISIBLE_OUTLINE"]
        ext_len = cline.length() - cline.intersection(visible_bound).length()
        assert 15.0 <= ext_len <= 35.0, "Error: 中心线伸出轮廓长度不合国标！"
        
    # 断言 3：剖面线零侵入性校验
    assert shapely_layers["HATCH_LINE"].buffer(0.001).intersection(shapely_layers["HOLES"]).area == 0.0, \
        "Critical Error: 剖面线非法穿入空气内腔！"
    return True
最终释放令牌：当且仅当 Python 隔离环境返回 True 且无任何 AssertionError 时，输出：[TOKEN: TOKEN_SYSTEM_RELEASE_RENDER]只有在输出了 TOKEN_SYSTEM_RELEASE_RENDER 之后，Agent 才可以生成最终的 Markdown 交付物标题、4~6条固化作图步骤、自检质量清单，并高清导出无损矢量图片。5. 异常拦截与状态回滚控制逻辑 (Exception & Rollback Log)如果在任何一个 GATE 阶段断言失败，系统必须立即挂起当前的绘图逻辑流，执行以下标准化惩罚性动作：禁止降级输出：严禁采用“虽然有错，但我肉眼修了一下”的模糊逻辑。强制回滚提示：输出错误日志 [STATE_EXCEPTION: GATE_X_FAILED]，指出具体的几何矛盾点或代数不守恒像素值。重构基准线：擦除当前关卡的局部坐标系，迫使 Agent 回退到 GATE 1 或上一级有效 Token 处，重新修正主锚点坐标 $(X_C, Y_C)$。